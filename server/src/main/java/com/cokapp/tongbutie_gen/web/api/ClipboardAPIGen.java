/*******************************************************************************
 * Service，重新生成不会覆盖
 * 
 * API For Clipboard
 * Auto Generated by CokGen At 2016-01-19 16:20:18
 *
 * Copyright (c) cokapp.com
 * Email: dev@cokapp.com
 *******************************************************************************/
package com.cokapp.tongbutie_gen.web.api;

import javax.servlet.http.HttpServletRequest;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.bind.annotation.ResponseBody;

import com.cokapp.tongbutie.entity.Clipboard;
import com.cokapp.tongbutie.service.ClipboardService;
import com.cokapp.quick.core.service.BaseService;
import com.cokapp.quick.core.web.controller.BaseController;
import com.cokapp.quick.core.web.view.JsonResult;

public abstract class ClipboardAPIGen extends BaseController<Clipboard, Long> {

	@Autowired
	private ClipboardService svr;

	@RequestMapping(value = { "" }, method = { RequestMethod.POST })
	@ResponseBody
	public JsonResult<Clipboard> create(@RequestBody Clipboard entity) {
		this.getService().save(entity);
		return JsonResult.newSuccess(entity, "新建成功！");
	}

	@RequestMapping(value = "/{id}", method = { RequestMethod.DELETE })
	@ResponseBody
	public JsonResult<Clipboard> delete(@PathVariable("id") Long id) {
		this.getService().delete(id);
		return JsonResult.<Clipboard> newSuccess("删除成功！");
	}

	@Override
	protected BaseService<Clipboard, Long> getBaseService() {
		return this.svr;
	}

	protected ClipboardService getService() {
		return this.svr;
	}
	
	@RequestMapping(value = { "" }, method = { RequestMethod.GET })
	@ResponseBody
	public JsonResult<Clipboard> list(HttpServletRequest request,
			@RequestParam(value = "paged", required = false, defaultValue = "false") Boolean paged) {
		JsonResult<Clipboard> json = JsonResult.newSuccess();
		if (paged) {
			json.setData(this.findPage(request));
		} else {
			json.setData(this.findList(request));
		}
		return json;
	}
	
	@RequestMapping(value = { "/{id}" }, method = { RequestMethod.POST })
	@ResponseBody
	public JsonResult<Clipboard> update(@RequestBody Clipboard entity, @PathVariable("id") Long id) {
		entity.setId(id);
		this.getService().update(entity);
		return JsonResult.newSuccess(entity);
	}

	@RequestMapping(value = { "/{id}" }, method = { RequestMethod.GET }, produces = "application/json")
	@ResponseBody
	public JsonResult<Clipboard> view(@PathVariable("id") Long id) {
		Clipboard entity = this.getService().findOne(id);
		if (entity == null) {
			return JsonResult.newError("没有找到ID(%s)对应的记录！", id);
		}		
		return JsonResult.newSuccess(entity);
	}

}





